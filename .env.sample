DB_HOST=localhost
DB_PORT=3306
DB_USER=username     # Замените на вашего пользователя MySQL
DB_PASSWORD=password # Замените на ваш пароль MySQL
DB_NAME=tree_fungi

# Составление URL подключения из переменных
DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"
